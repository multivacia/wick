# Especificações de Tarefas de IA

Cada tarefa deve possuir um identificador único.

Formato recomendado:

```text
<AREA>-<ASSUNTO>-<NÚMERO>
```

Exemplo:

```text
R3E-READINESS-001
```

Arquivo:

```text
<TASK_ID>_SPEC.md
```

Use o template em `templates/AI_TASK_SPEC_TEMPLATE.md`.
