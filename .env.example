# Wick environment example — copy to .env and adjust.
# Never commit real secrets.

DATABASE_URL=postgresql+psycopg://wick:wick@localhost:5432/wick
CANDLE_CLOSE_SAFETY_DELAY_SECONDS=30
LOG_LEVEL=INFO

# Optional — only needed for broader Brazilian equity coverage via brapi
# BRAPI_TOKEN=

# Optional TimescaleDB (docker compose --profile timescale)
# DATABASE_URL=postgresql+psycopg://wick:wick@localhost:5433/wick
